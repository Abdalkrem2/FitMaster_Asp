<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- Only the EF Core abstractions package (for DbSet<T> in IApplicationDbContext) -
         NOT the SQL Server provider, which stays in Infrastructure. -->
    <PackageReference Include="Microsoft.EntityFrameworkCore" Version="10.0.0" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FitMaster.Domain\FitMaster.Domain.csproj" />
  </ItemGroup>

</Project>
